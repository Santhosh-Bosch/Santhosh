#ifndef UTILS_H
#define UTILS_H

// Function prototypes for utility functions
void formatTelemetryData(char* buffer, const char* dataType, float value);
void handleError(const char* errorMessage);

#endif // UTILS_H